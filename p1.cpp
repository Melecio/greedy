#include <cstdio>
#include <vector>
#include <algorithm>
using namespace std;



int main(void) {
    int casos;
    scanf("%d", &casos);

    while(t--) {
        int offs, dist, modems, creg, cesp;
        scanf("%d %d %d %d %d");

        for (int i = 0; i < offs; ++i) {
            scanf("%d %d");

        }

    }
}
